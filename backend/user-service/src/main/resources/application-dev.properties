# Profil DEV : valeurs de commodité pour le développement local et la stack docker-compose dev.
# Ces valeurs ne sont JAMAIS chargées en prod (profil prod). Ne pas y mettre de secret réel.

# ─── Secrets de dev (non sensibles, usage local uniquement) ────────────────────
spring.datasource.password=root
fintrack.jwt.secret=C003FA3DC88493379ABFCC2779594D1MYSECRETKEY1233ADMIN
fintrack.internal-service.token=fintrack-internal-dev-token
fintrack.security.temp-password-secret=${fintrack.jwt.secret}

# ─── CORS : Vite local ─────────────────────────────────────────────────────────
fintrack.allowed-origins=http://localhost:5173,http://localhost:5174

# ─── Swagger activé en dev ─────────────────────────────────────────────────────
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
