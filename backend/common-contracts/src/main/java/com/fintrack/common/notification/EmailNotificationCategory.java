// Constantes metier : regroupe les evenements e-mail par famille fonctionnelle
// (pilote uniquement le classement dans l'UI Super Admin).

package com.fintrack.common.notification;

// Familles d'e-mails configurables.
public enum EmailNotificationCategory {
  LIFECYCLE,
  SCHEDULED,
  DIRECTION,
}
