// Centralise les types communs des composants d'interface.
export type ComponentSize = "sm" | "md" | "lg";

// Centralise la logique d'interface liee a antd size.
export type AntdSize = "small" | "middle" | "large";

// Centralise la logique d'interface liee a antd spin size.
export type AntdSpinSize = "small" | "default" | "large";

// Centralise la logique d'interface liee a button variant.
export type ButtonVariant =
  | "primary"
  | "secondary"
  | "danger"
  | "ghost"
  | "text";

// Centralise la logique d'interface liee a button html type.
export type ButtonHtmlType = "button" | "submit" | "reset";

// Centralise la logique d'interface liee a badge variant.
export type BadgeVariant = "low" | "medium" | "high" | "critical";
